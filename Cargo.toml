[package]
name = "kuramoto_sim"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.9"
csv = "1.3"
clap = { version = "4.0", features = ["derive"] }
eframe = { version = "0.30", optional = true }

[features]
default = ["viz"]
viz = ["eframe"]